# 此刻·此地 — 以相机为媒介的地点信件互动作品

> 抖音AI创变者大赛 · 赛道一 · 互动空间（轻量小游戏）

## 项目概述

**此刻·此地**是一个以相机取景器为核心界面的地点信件互动作品。

用相机在真实世界留下信，让别人走到那个地方、举起相机、对准你拍下的角度，信才会从取景器中浮现——完成一场跨越时空的"寻宝"。

### 三种信件

| 类型 | 描述 | 核心体验 |
|------|------|---------|
| **公开信** | 在任意地点留下感想，后来者都能发现 | 众人寻宝 — "我也来过这里" |
| **时光胶囊** | 写给未来自己的信，定时解锁，可多人共埋 | 穿越时间 — "一年后的你，还记得吗" |
| **密信** | 指定一个人，生成口令分享，Ta循着线索找到 | 两个人的秘密 — "课桌下的留言" |

### 核心交互

```
罗盘地图发现附近信封 → 走近目标地点 → 打开相机取景器
→ 对准原始拍摄角度 → 信封随对齐度逐渐清晰 → 展开阅读
→ 留下回响 / 写下新的信
```

## 技术栈

- 纯 HTML/CSS/JS
- Canvas 图像处理 + 传统CV特征提取（AI场景匹配）
- 大模型API（AI信件润色，完整版）
- Geolocation API + 地图SDK
- IndexedDB + LocalStorage

## 两阶段交付

| | 完整版 | Demo版（互动空间） |
|------|---------|-------------------|
| 包体 | 无限制 | ≤8MB |
| 网络 | 需要 | 纯离线 |
| 地图 | 高德/腾讯SDK | Leaflet + 预缓存瓦片 |
| AI润色 | 大模型API | 本地模板库 |
| 用途 | 游园现场展示 | 互动空间投票 |

## 目录结构（规划）

```
src/
├── index.html
├── css/
│   └── main.css
└── js/
    ├── app.js                   # 主路由与初始化
    ├── config.js                # 配置常量
    ├── core/
    │   ├── letter-manager.js    # 信件创建/查询/管理
    │   ├── feature-engine.js    # AI场景特征提取与比对
    │   └── location-service.js  # GPS定位与距离计算
    ├── ui/
    │   ├── map-view.js          # 地图罗盘视图
    │   ├── camera-view.js       # 相机取景器视图
    │   ├── letter-composer.js   # 写信界面
    │   └── letter-reader.js     # 读信界面
    ├── data/
    │   ├── storage.js           # IndexedDB + LocalStorage 封装
    │   └── templates.js         # AI润色降级模板库
    └── utils/
        └── helpers.js           # 工具函数
```

## 设计文档

完整设计文档见 `docs/superpowers/specs/2026-05-30-此刻此地-design.md`

## 开发日志

见 `docs/开发日志.md`
